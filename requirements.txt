# ── Core CV ──────────────────────────────────────────────
ultralytics>=8.0.0          # YOLOv8
opencv-python>=4.9.0        # camera capture + HUD drawing
mediapipe==0.10.9           # Face Mesh / gaze estimation (0.10.14+ removed mp.solutions on Apple Silicon)

# ── Scientific ────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0               # Shannon entropy for adaptive sampler

# ── macOS Integration ─────────────────────────────────────
pyobjc-core>=10.0           # Obj-C bridge
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-ApplicationServices>=10.0
pyobjc-framework-Quartz>=10.0
rumps>=0.4.0                # Menu bar app

# ── Data / Storage ────────────────────────────────────────
# sqlite3 is stdlib — no install needed

# ── Reporting ─────────────────────────────────────────────
rich>=13.0.0                # pretty CLI tables / ASCII charts
jinja2>=3.1.0               # HTML report templating
pyyaml>=6.0.0               # config.yaml loader

# ── Dev / Testing ─────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
